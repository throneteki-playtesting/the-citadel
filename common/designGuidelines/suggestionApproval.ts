// A starting guess, not confirmed by data - a suggestion needs at least this many likes to count
// as having enough community support to be worth an approval decision.
export const SUGGESTION_APPROVAL_VOTE_THRESHOLD = 3;
