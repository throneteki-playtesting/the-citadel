// Shared label sizing so filter rows (toggle groups, boolean toggles, etc.) line up in a
// consistent left column regardless of how long each individual label is. Stacks above the
// control on narrow screens, sits beside it (fixed width, for alignment) from sm: up.
export const FILTER_LABEL_CLASS = "text-xs text-default-500 shrink-0 sm:w-16";
export const FILTER_ROW_CLASS = "flex flex-col sm:flex-row sm:items-center gap-1 sm:gap-2";
