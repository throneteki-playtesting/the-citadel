import { SuggestionFilterValue } from "../../components/data/suggestionFilter";

// A short, hand-maintained list of current gaps worth calling out - not auto-detected, since there's
// no good definition of "sparse" yet. Add/remove entries here by hand as priorities shift.
export const FOCUS_AREAS: { label: string; filter: SuggestionFilterValue }[] = [];
